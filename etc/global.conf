# Some default settings (uncomment if you want to change)
HOSTNAME=`hostname -f`
USER="mirror"
MAILTO="mirror@localhost"
LOGROTATE=30
#ERRORSONLY=true
#FULLLOGS=false
#SIMPLE_RSYNC_OPTIONS="-avH --delete-after --delay-updates"

# Better make this repo specific
#SIMPLE_RSYNC_EXTRA=""

## Below is only for demostration. Set it in the PER-REPO conf!
## REPO should be the command line argument
###############################################################
#LOGDIR="${HOME}/log/${REPO}"
#LOGFILE="${LOGDIR}/${NAME}.log"
#TO="${HOME}/root/${REPO}"
#LOCKFILE="${REPODIR}/Archive-Update-in-Progress-${HOSTNAME}"
